import React from "react";

export const Layout = ({ children }) => (
  <div
    style={{
      // maxWidth: "60vw",
      padding: "40px",
      margin: "auto",
      backgroundColor: "red",
    }}
  >
    {children}
  </div>
);
