 //grid maybe
 import React from "react";

export const Layout = ({ children }) => (
  <div
    style={{
      // maxWidth: "60vw",
      padding: "20px",
      margin: "auto",
      backgroundColor: "blue",
    }}
  >
    {children}
  </div>
);
